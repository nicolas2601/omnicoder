# Learned Patterns

- Prefer explicit types over `any` in TypeScript
- Always batch related file operations in a single tool call
- Use `bun test` instead of jest for this workspace
- Never commit .env files or secrets
- Use ES modules (.js extensions in imports)
